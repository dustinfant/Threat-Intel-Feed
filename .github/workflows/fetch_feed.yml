name: Fetch Threat Intel Feed

# Run every hour + manual trigger
on:
  schedule:
    - cron: '0 * * * *'  # every hour
  workflow_dispatch:  # manual trigger

permissions:
  contents: write  # allows committing updated feed

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Set up Python 3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3️⃣ Cache Python packages
      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyyaml requests beautifulsoup4

      # 5️⃣ Run the fetcher safely
      - name: Run threat intel fetcher
        run: |
          set -e
          echo "Starting fetcher..."
          python - <<'EOF'
import sys
try:
    import fetcher.fetch_normalized_auto as fetcher
    summary = fetcher.run_all()  # returns dict summary of fetch
    import json
    with open("data/fetch_summary.json", "w") as f:
        json.dump(summary, f, indent=2)
except Exception as e:
    print("Fetcher encountered an error:", e, file=sys.stderr)
    sys.exit(0)  # always exit 0 so workflow continues
EOF

      # 6️⃣ Commit updated feeds and summary
      - name: Commit updated feeds
        run: |
          git config --local user.name "github-actions"
          git config --local user.email "actions@github.com"
          git add data/feed_normalized.json data/fetch_summary.json
          git diff-index --quiet HEAD || git commit -m "Update feed_normalized.json and fetch_summary.json"
          # Pull remote first to avoid rejected push
          git pull --rebase origin main
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
